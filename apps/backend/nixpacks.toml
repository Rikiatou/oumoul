[phases.setup]
nixPkgs = ["...", "nodejs_20", "openssl"]

[phases.build]
cmds = [
  "cd ..",
  "npm install --workspace=apps/backend --include-workspace-root",
  "cd apps/backend",
  "npx prisma generate",
  "npm run build"
]

[phases.start]
cmds = ["npx prisma migrate deploy", "npm run start:prod"]

[variables]
NODE_ENV = "production"
